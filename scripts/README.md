# Commands

## Gridcal

```bash
python -u scripts/main server=jz experiment=baseline model.kernels.kernel="rbf"
```


---

## JeanZay

```
```


```bash
python -u scripts/main server=jz experiment=baseline model.kernels.kernel="rbf"
```